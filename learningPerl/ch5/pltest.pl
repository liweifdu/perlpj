#!/usr/bin/perl

use strict;

# $line = <STDIN>;
# chomp($line);
# chomp($line = <STDIN>);

# while read a line once a time
# while (<STDIN>) {
#     print "I saw $_";
# }
# foreach read whole file once a time
# foreach (<STDIN>) {
#     print "I saw $_";
# }

# while (<>) {
#     chomp;
#     print "It was $_ that I saw!\n";
# }
# print <>;
# print sort <>;

# printf "Hello, %s; your password expires in %d days!\n",
#     $user, $days_to_die;

# open CONFIG, 'dino';
# open CONFIG, '<dino';
# open BEDROCK, '>fred';
# open LOG, '>>logfile';
